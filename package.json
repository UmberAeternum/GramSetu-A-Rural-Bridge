{
  "name": "gramsetu",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap": "capacitor",
    "cap:add:android": "capacitor add android",
    "cap:add:ios": "capacitor add ios",
    "cap:open:android": "capacitor open android",
    "cap:open:ios": "capacitor open ios",
    "cap:sync": "capacitor sync",
    "cap:build:android": "npm run build && capacitor sync android",
    "cap:build:ios": "npm run build && capacitor sync ios"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.21"
  },
  "dependencies": {
    "@capacitor/core": "^5.5.0",
    "@capacitor/cli": "^5.5.0"
  }
}
